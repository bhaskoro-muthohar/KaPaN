version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
  build:
    commands:
      - echo "Build phase"
  post_build:
    commands:
      - echo "Post-build phase"
      - echo "Post-build completed on `date`"

artifacts:
  files:
    - '**/*'
  discard-paths: no